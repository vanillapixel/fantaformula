### Fantasy Formula Backend Environment Example
# Copy to backend/.env and adjust as needed.

# Absolute or relative path (relative resolved from config.php directory) to SQLite database file
DB_PATH="db_location_here"

# JWT secret (change in production!)
JWT_SECRET="your_key_here/change_me"

# App environment: development | production
APP_ENV=development
